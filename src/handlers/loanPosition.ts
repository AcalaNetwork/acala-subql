import { EventHandler } from "./types";
import { LoanPosition } from "../types/models";

export const createLoanPosition: EventHandler = async ({ event, rawEvent}) => {

}

export const createGlobalLoanPosition: EventHandler = async ({ event, rawEvent }) => {

}