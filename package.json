{
  "name": "acala-subql",
  "version": "1.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "prepack": "rm -rf dist && npm build",
    "codegen": "./node_modules/.bin/subql codegen",
    "patch:types": "node scripts/patch-types.js",
    "start": "./node_modules/.bin/subql-node -f . --local --timeout=512 --batch-size=100"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "qwer951123",
  "license": "Apache-2.0",
  "devDependencies": {
    "@acala-network/types": "^2.0.2",
    "@polkadot/api": "^5.2.1",
    "@subql/cli": "^0.9.3",
    "@subql/node": "^0.19.1",
    "@subql/types": "^0.8.0",
    "typescript": "^4.1.3",
    "yaml": "^1.10.0"
  },
  "dependencies": {
    "@acala-network/sdk-core": "^2.0.2",
    "@open-web3/orml-types": "^0.9.3"
  }
}
